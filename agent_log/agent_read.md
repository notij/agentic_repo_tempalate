# 项目模块说明（给后续 Agent）

## 固定规则（给 Agent）

- 读取目的：先阅读本文件，快速理解**整个 repo 的功能、模块边界与文件职责**，用于工作交接与上下文续接。
- 执行方式：默认仅基于本文件与 `agent_log/worklog` 完成交接，不要求进入具体代码文件；仅当任务明确要求实现/排障时再读源码。
- commit 前约束：每次执行 commit 前，必须先按 `agent_log/agent_update.md` 的规则完成更新任务。

## 模块总览
